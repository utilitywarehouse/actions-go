inputs:
  go-version:
    description: 'The Go version to download (if necessary) and use. Supports semver spec and ranges.'
    required: true
  go-stable:
    description: 'Whether to download only stable versions of Go'
    default: 'true'

runs:
  using: "composite"
  steps:
    - name: Setup Go
      uses: actions/setup-go@v2
      with:
        go-version: ${{ inputs.go-version }}
        stable: ${{ inputs.go-stable }}
    - id: short-sha
      run: echo "::set-output name=sha::$(echo ${GITHUB_SHA} | cut -c1-8)"
      shell: bash
    - name: Setup cache
      uses: actions/cache@v2
      with:
        path: |
          ~/.cache/go-build
          ~/go/pkg/mod
        key: go-${{ inputs.go-version }}-${{ github.workflow }}-${{ github.job }}-${{ github.ref }}-${{ steps.short-sha.outputs.sha }}
        restore-keys: |
          go-${{ inputs.go-version }}-${{ github.workflow }}-${{ github.job }}-${{ github.ref }}-${{ steps.short-sha.outputs.sha }}
          go-${{ inputs.go-version }}-${{ github.workflow }}-${{ github.job }}-${{ github.ref }}-
          go-${{ inputs.go-version }}-${{ github.workflow }}-${{ github.job }}-refs/heads/master-
          go-${{ inputs.go-version }}-${{ github.workflow }}-${{ github.job }}-
